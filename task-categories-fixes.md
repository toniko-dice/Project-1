# Задача: доработки след task-categories-tree.md

Проверено на живо: дървото, `/kategorii/delta-seriya` с раздели и
бройки, адресите на продуктите, пренасочванията от старите, трохите,
canonical, BreadcrumbList/ItemList, sitemap (47 адреса, нула стари) —
всичко работи. Остават шест дребни неща.

## 1. Панелът още изписва стари адреси

В HTML-а на менюто: „Виж всички → `/categories/delta-seriya`“ и
„→ `/categories/aksesoari`“. Работи през пренасочването, но заданието
беше нула стари адреси в HTML-а. Причина: `viewAllUrl` в секциите не е
изчистен при миграцията и има предимство пред `viewAllCategory`.
Миграция с данни: за всяка секция, чийто `viewAllUrl` започва с
`/categories/` или `/products/` → изпразни го (категорията вече е в
`viewAllCategory`). Същото за `viewAllTileUrl`.

## 2. Изтрита категория още е в менюто

В хедъра има линк „Домашни батерии → `/categories/domashni-baterii`“
— категорията беше изтрита по §1, но записът в `header` (група или
entry) остана. Намери го и го махни/пренасочи към правилната категория
(`powerocean` или `stream-seriya` — виж `content/kategorii.md`).
Провери и другите четири изтрити (`vanshni-baterii`, `generatori`,
`inteligenten-dom`, `klimatici`) — нито един линк в хедъра, футъра,
началната страница и панелите не бива да сочи към тях.

## 3. Заглавие на прозореца — двойна наставка

`DELTA серия — EcoFlow България — EcoFlow България`. Наставката се
добавя два пъти (`metadata.title` + `title.template` в layout).
Оставя се само template-ът.

## 4. Мета описание липсва

Категорията има `description`, но `<meta name="description">` не се
изписва. `generateMetadata`: `metaDescription || description`.
Освен това описанието на DELTA серия сега е „панелът „Серия EcoFlow
DELTA“ в менюто“ — това е бележка от `kategorii.md`, не текст за
посетители. Seed-ът да не попълва `description` от бележките — да
остава празно; собственикът ще напише текста от админа. Изчисти
описанията, които са дошли от бележки (всички категории).

## 5. Празен банер

При категория без `banner` в горната част стои сива плочка „Няма
снимка“. Без банер — не се показва нищо (само заглавие и описание,
без празно поле).

## 6. Трохата на подсерията

На продуктовата страница: „DELTA 3 серия → `/kategorii/delta-3-seriya`“.
Подсерията няма собствена страница (§5 на предишното задание) —
трохата да води към `/kategorii/delta-seriya?sub=delta-3-seriya`, а
`/kategorii/delta-3-seriya` да пренасочва натам (308), ако някой го
отвори.

## 7. Проверка

`view-source` на началната и на `/kategorii/delta-seriya`: нула
срещания на `/categories/` и `/products/`; title без двойна наставка;
meta description = описанието; без сива плочка при празен банер; на
`/kategorii/delta-seriya/delta-3` трохата „DELTA 3 серия“ води към
страницата на DELTA серия с активен раздел DELTA 3.

## 8. Git

Commit + push.
